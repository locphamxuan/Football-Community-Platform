# === AI tooling / skills (KHÔNG push lên GitHub) ===
# Skill UI/UX Pro Max cài vào project qua `uipro init` — chỉ dùng cục bộ
.claude/

# claude-mem (nếu tạo file cục bộ trong project)
.claude-mem/
claude-mem.db*

# Thư mục clone repo AI dùng để tham khảo cục bộ (nếu có)
ai-tools/
external-skills/

# CodeGraph: index cục bộ của từng máy, cộng file cấu hình MCP.
.codegraph/
.mcp.json

# Tài liệu/hướng dẫn dành cho công cụ AI — không public qua repo.
CLAUDE.md
**/CLAUDE.md
AGENTS.md
**/AGENTS.md
docs/09-codegraph.md
memory/

# === Chung ===
node_modules/
