# Local dev trên giả lập Android dùng mặc định 10.0.2.2 (xem mobile/src/lib/api.ts), không
# cần set biến này. Chỉ bắt buộc khi chạy trên máy thật hoặc build APK/production trỏ tới
# backend thật — thiếu thì build release sẽ gọi nhầm vào 10.0.2.2 (chỉ tồn tại trong giả lập).
EXPO_PUBLIC_API_URL=https://your-backend.onrender.com/api/v1
